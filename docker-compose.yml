# Build the hub and all sub-apps in one compose setup.
# Each sub-app builds independently; the hub collects their outputs.

services:
  apps-hub:
    build:
      context: .
      dockerfile: Dockerfile.combined
    ports:
      - "8080:80"
